# QM-MM Inhibition Pipeline — Requirements
# Install: pip install -r requirements.txt

# Core quantum chemistry
pyscf>=2.8.0
gpu4pyscf-cuda12x>=1.8.0      # For NVIDIA GPU acceleration (CUDA 12+)

# Cheminformatics
rdkit>=2024.3.0
biopython>=1.84               # PDB parsing (replaces MDAnalysis)

# Scientific computing
numpy>=1.24,<2.0
scipy>=1.10.0
pandas>=2.0.0

# Visualization
matplotlib>=3.7.0

# Steric descriptors (optional)
morfeus-ml>=0.8.0
